#
# Replace this file with the app.conf from your CURRENTLY DEPLOYED
# Sigma_Alerts app before the first build, so app metadata stays continuous
# at the clients. The version line is rewritten from the VERSION file at the
# repository root, so whatever is here is overwritten.
#
[install]
is_configured = 0
state = enabled

[ui]
is_visible = 1
label = Sigma Alerts

[launcher]
author = Secureon d.o.o.
description = sigma rules clone
version = 0.0.0
